# MCP Orch 환경 설정 예시 파일
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요

# 애플리케이션 환경 (development, production)
ENV=development

# 서버 설정
SERVER__HOST=0.0.0.0
SERVER__PORT=8000
SERVER__MODE=proxy
SERVER__LOG_LEVEL=INFO

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=change-me-in-production
DB_NAME=mcp_orch
# 직접 DATABASE_URL을 지정하려면 아래 주석을 해제하세요
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/mcp_orch
SQL_ECHO=false

# 보안 설정
SECURITY__ENABLE_AUTH=true

# JWT 시크릿 (프로덕션에서는 반드시 강력한 값으로 변경)
AUTH_SECRET=your-secret-key-here-change-in-production

# 초기 관리자 계정 설정 (서버 시작 시 자동 생성/권한 부여)
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=change-me-in-production

# Next.js Frontend API URL
# 로컬 개발: http://localhost:8000
# Docker 컨테이너: http://mcp-orch-backend:8000
NEXT_PUBLIC_MCP_API_URL=http://localhost:8000

# Docker Compose 설정 (선택사항)
# COMPOSE_PROJECT_NAME=mcp-orch